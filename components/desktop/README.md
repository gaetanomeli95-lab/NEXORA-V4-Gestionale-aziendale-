# Desktop Runtime Components

- `desktop-runtime-provider.tsx`: legge lo stato runtime Tauri e mostra watermark/overlay demo.
- `desktop-feature-guard.tsx`: helper per nascondere o sostituire feature bloccate in demo scaduta.

Questi componenti sono volutamente minimi e servono come base per collegare i controlli di licensing alle feature applicative sensibili.
